#!/bin/bash

export CLASSPATH=$(pwd)/Tests/classes/*:$(pwd)/lib/javassist/javassist.jar:$(pwd)/classes/

ant clean;ant compile;ant testsCompile

for i in $(ls Tests/classes/Test* | rev | cut -b 7- | rev|cut -b 15-);do echo -e "starting test for $i\n";java -jar lib/jkargs/jkargs.jar $i;done

  
